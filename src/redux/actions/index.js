/* import getCurrency from "../../services/apiRequest";
export const USER_EMAIL = 'USER_EMAIL';
export const CURRENCY = 'USER_EMAIL';

export const getMail = (email) => ({
  type: USER_EMAIL,
  payload: {
    email,
  },
});

export const getMail = (email) => ({

} */
